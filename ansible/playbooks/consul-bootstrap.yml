---
- name: Install and configure Consul
  hosts: all
  become: yes
  vars:
    consul_ansible_master_group_name: "consul_masters"
    secrets_dir: "/home/n43/Documents/proxmox-services/secret"
    consul_ca_info:
      C: "FR"
      ST: "LORRAINE"
      L: "Epinal"
      O: "splitfire"

  roles:
    - consul-bootstrap