---
http:
  routers:
    to-argocd:
      rule: "Host(`argocd-staging.korporate.org`)"
      service: argocd

  services:
    argocd:
      loadBalancer:
        servers:
          - url: https://10.0.50.21:8443
