---
http:
  routers:
    to-sonarqube:
      rule: "Host(`sonarqube.korporate.org`)"
      service: sonarqube
      middlewares:
        - "chain-basic-auth"

  services:
    sonarqube:
      loadBalancer:
        servers:
          - url: 	http://10.0.20.22:9001
